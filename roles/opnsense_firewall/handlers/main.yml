---
# Applying is the moment a policy mistake becomes an outage, so this is no
# longer a bare POST. See tasks/apply_rules.yml.
- name: apply firewall rules
  ansible.builtin.include_tasks: apply_rules.yml
