---
# Configure OPNsense DHCP for substrate
#
# Secrets are vaulted in inventory (group_vars/routers/vault.yml).
# Usage:
#   ansible-playbook playbooks/dhcp.yml --ask-vault-pass

- name: Configure OPNsense DHCP
  hosts: dhcp_servers
  gather_facts: false
  roles:
    - opnsense_dhcp
