---
# Configure OPNsense DNS for substrate
#
# Secrets are vaulted in inventory (group_vars/routers/vault.yml).
# Usage:
#   ansible-playbook playbooks/dns.yml --ask-vault-pass

- name: Configure OPNsense DNS
  hosts: dns_servers
  gather_facts: false
  roles:
    - opnsense_dns
